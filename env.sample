# TikTok host to capture (username without @)
TIKTOK_HOST=mockhost

# ClickHouse connection string
CLICKHOUSE_CONN=Host=192.168.1.230;Port=8123;Database=chakal;User=default;Password=Chakal123!

# Log level (Debug, Information, Warning, Error, Critical)
LOG_LEVEL=Debug

# Enable debug mode which generates mock data
DEBUG_MODE=true 